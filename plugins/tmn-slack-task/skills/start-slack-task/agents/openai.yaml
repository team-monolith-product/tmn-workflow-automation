interface:
  display_name: "Start Operations Task"
  short_description: "운영팀 Slack List 작업의 시작과 종료 결과를 공유합니다"
  default_prompt: "Use $start-slack-task to start this operations Slack List task and publish a concise result when the work is actually finished."
dependencies:
  tools:
    - type: "mcp"
      value: "team-monolith-operations-task"
      description: "운영팀 Slack List 작업 맥락을 읽고 작업 결과를 게시하는 전용 MCP"
      transport: "streamable_http"
      url: "https://slack-task-mcp.invalid/mcp"
policy:
  allow_implicit_invocation: true
