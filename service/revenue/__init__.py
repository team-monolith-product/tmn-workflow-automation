"""
매출 facts 서비스.

매출장 구글시트 하나가 금액의 유일한 출처다. 여기서는 그 시트를 읽어(ledger)
분류 규칙을 입히고(taxonomy) 집계·검증한 결과(build)를 facts 라는 dict 하나로
만든다. MCP 도구·HTTP 엔드포인트·스케줄 작업이 전부 같은 facts 를 본다.

분류 어휘를 여기서 새로 만들지 않는다. 대분류·세부분류의 정본은 매출장
「분류마스터」 탭이고, knowledge/revenue/taxonomy.yml 은 2025년 이하 옛 분류를
그 체계로 환산하는 규칙일 뿐이다.
"""
